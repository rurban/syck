[WARNING: this file is outdated]

## Syck 0.60

[x] Resolver needs to handle implicits
[x] yaml_org_handler is an integral part of DefaultResolver#transfer
[x] cNode must be central in-between datatype
  [x] The values will be fully loaded Hash, Array, String
      (Close, ended up using YAML::Syck::Seq, etc.)
  [x] This way node_import can take pure Ruby nodes
  [x] Incorporate BaseNode.
[-] Fix typed seq-in-map shortcut.
[x] Emitter should figure out shortcuts.
[-] Ensure that common memory leaks are GONE by 0.60.
[-] UTF-8 strict mode.

## Syck 0.70

[ ] Check the !! shorthand. !binary becomes !!binary
[ ] Check if the %YAML 1.0 directive is allowed (space not :
    for forward compat)
[ ] Check if a ^ prefixed tag is allowed (but ignored)
[x] All memory leaks are GONE
[ ] Security extensions from perl: max recursion depth.
[ ] Policies from perl: allow/disallow duplicate keys
[ ] Specialize perl symbol quoting rules
[ ] UTF-8 strict mode
[ ] Fix typed seq-in-map shortcut.

## Syck 0.80

[ ] Support old character indicators? (1.0 only), ie % for imap, @ for iseq, ...
    1.1 ? map keys
[ ] Support unnamed tagged seq's and map's
[ ] Allow implicit entries in flow sequences without the - indicator
[ ] Update boolean recognition (YAML 1.2 only recognizes true/false, not yes/no/on/off)
[ ] Add a version config (see above)
[ ] Fix octal number parsing (YAML 1.2 uses 0o prefix, not leading 0)
[ ] Update the implicit tag resolution rules
[ ] UTF-16 support for 1.0 (via BOM)

## Syck 0.90

[ ] Python extension with complete hooks.
[ ] Full YAML 1.1 and 1.2 support.
[ ] UTF-16 and UTF-32 support for 1.2 (via BOM)
